{
  "name": "youtube-mp3-local-server",
  "version": "1.0.0",
  "private": true,
  "description": "Local backend that runs yt-dlp so music.html can convert YouTube links to MP3 without a third-party API.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "cors": "^2.8.5"
  }
}
